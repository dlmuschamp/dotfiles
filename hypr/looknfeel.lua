-- Personal rice overrides, ported from the published looknfeel.conf.
hl.config({
  general = {
    gaps_in = 0,
    gaps_out = 0,
    border_size = 1,
  },

  decoration = {
    rounding = 0,
    active_opacity = 0.95,
    inactive_opacity = 0.95,
    dim_inactive = true,
    dim_strength = 0,
  },

  animations = {
    enabled = true,
  },
})
