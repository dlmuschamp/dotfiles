#!/bin/bash

set -euo pipefail

pgrep -x hyprlock >/dev/null && exit 0

# Return to the primary keyboard layout before authentication.
hyprctl switchxkblayout all 0 >/dev/null 2>&1 || true
exec hyprlock --config "$HOME/.config/hypr/hyprlock.conf"
